import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'interpolation-pipes-events',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
